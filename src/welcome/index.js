/**
 * @file welcome
 * @author cuiyongjian(cuiyongjian@baidu.com)
 */

define(function (require) {

    var config = {};

    config.view = require('./indexView');

    config.model = require('./indexModel');

    return config;

});
