/**
 * @file 
 * @author ()
 */

define(function (require) {

    var config = {};

    config.template = require('./detail.tpl');

    /**
     * 渲染页面
     * @param data
     */
    config.render = function (data) {

    };

    return config;

});
