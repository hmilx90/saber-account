/**
 * saber-lang
 *
 * @file  lang
 * @author  firede[firede@firede.us]
 */

define(function (require) {

    return {
        inherits: require('./inherits'),
        extend: require('./extend'),
        curry: require('./curry'),
        bind: require('./bind')
    };

});
