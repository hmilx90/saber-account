define(function() {

    var widget = require( 'saber-widget' );

    describe( 'dispose', function() {

        it( 'should have method `dispose`', function () {
            expect( typeof widget.dispose ).toEqual( 'function' );
        });

    });

});
